# J16-ISA
This repository contains the current integrated checkpoint for J16, a deterministic, forward-only, certifiable execution architecture built around bounded processes, explicit capabilities, and symbol-bank composition.
